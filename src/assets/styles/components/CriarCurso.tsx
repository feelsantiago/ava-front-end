import React from 'react';
import styled from 'styled-components';
import { Button } from 'antd';

export const CriarCursoSubmitButton = styled((props) => <Button {...props} />)`
    width: 225px;
`;
